apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maven-local-repo
  namespace: ${ENVIRONMENT}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
